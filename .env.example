# MongoDB
DATABASE_MDB=

# JWT (mismo secret que pms-core)
JWT_SECRET=

# Server
PORT=4040
NODE_ENV=development

# Core API (PMS principal — NO es esta app)
# En producción: URL del backend pms-core donde se gestionan properties
# En desarrollo: se salta la verificación (NODE_ENV=development)
CORE_API_URL=http://localhost:3030
